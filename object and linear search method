using System;

namespace ConsoleApp1
{
    class Program
    {
        static void Main(string[] args)
        {
            mobile phone = new mobile();
            car car = new car();
           
            phone.message("","");
            
            car.drive();//c.drive(c)
            
            
            static int linearSearch(int []array,int x)//return the index of x is .
            {
                for (int i = 0; i < array.Length; i++)
                {
                    if (array[i]==x)
                    {
                        return i;
                    }
                }
                return -1;
            }

        }
    }
    class mobile
    {
        public string name;
        public int serialNo;
        public string brand;
        public string color;
        public double price;

        public void call() { }
        public void message(string text, string phoneNo) {}
        
        
    }
    class car
    {
        public string company;
        public string name;
        public string speed;
        public int serialNo;

        public void drive() // public void drive(this)
        { 
        }

    }
    
}
